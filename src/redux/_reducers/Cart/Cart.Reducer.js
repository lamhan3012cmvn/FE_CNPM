/* eslint-disable import/no-anonymous-default-export */

import {} from "../../_actions/Cart/type"

const initState = {}
export default function (state = initState, action) {
  switch (action.type) {
    default:
      return state
  }
}
